import React, { FC, PropsWithChildren } from 'react'

export const Header : FC<PropsWithChildren> = ({children}) => {
    return(
        <header>
            <div>
                NADA SVENDBORG
            </div>
            <div>

            </div>
        </header>
    )
}